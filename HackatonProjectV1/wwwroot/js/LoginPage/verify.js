document.addEventListener('DOMContentLoaded', () => {
    // 3 Saniye bekle ve sonra giriş sayfasına yönlendir
    setTimeout(() => {
        // İsteğe bağlı: İşlem bittiğinde başarılı mesajı verip öyle de yönlendirebiliriz
        // Ama şimdilik direkt yönlendiriyoruz
    }, 3000); // 3000 milisaniye = 3 saniye
});